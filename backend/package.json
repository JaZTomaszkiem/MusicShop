{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "dist/index.js",
    "prestart": "npm run -s build",
    "dev": "nodemon -w server/app.js --exec \"babel-node server/app.js\"",
    "build": "rimraf ./dist && babel ./server -d ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "@babel/cli": "^7.16.0",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^10.0.0",
    "express": "~4.16.1",
    "mongoose": "^6.0.13",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "swagger-jsdoc": "^6.1.0",
    "swagger-ui-express": "^4.1.6"
  },
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/node": "^7.16.0",
    "@babel/preset-env": "^7.16.4",
    "babel-preset-env": "^1.7.0"
  }
}
